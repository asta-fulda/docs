version: '2'
services:
    jekyll:
        build: .
        image: astadocs
        volumes:
            - './:/src'
        restart: always
        ports:
            - '4000:4000'
        command: serve -H 0.0.0.0